dependencies:
  pre:
    - echo y | android update sdk --no-ui --all --filter "build-tools-23.0.3"
test:
  override:
    - cd ares; ./gradlew test
notify:
  webhooks:
    - url: http://mcc.aphelion.io/builds/3/hook/circle
